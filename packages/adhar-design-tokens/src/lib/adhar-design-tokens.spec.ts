import { adharDesignTokens } from './adhar-design-tokens';

describe('adharDesignTokens', () => {
  it('should work', () => {
    expect(adharDesignTokens()).toEqual('adhar-design-tokens');
  });
});
